[project]
name = "amelidash"
version = "0.1.0"
description = "Dashboard AMELI – analyse des dépenses et effectifs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl==3.1.3",
    "pandas==3.0.3",
    "loguru",
]
